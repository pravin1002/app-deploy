name: deploy-app

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize

  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Print deployment message
        run: echo "Deploying app..."
